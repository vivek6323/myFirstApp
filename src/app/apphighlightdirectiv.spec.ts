import { Apphighlightdirectiv } from './apphighlightdirectiv';

describe('Apphighlightdirectiv', () => {
  it('should create an instance', () => {
    const directive = new Apphighlightdirectiv();
    expect(directive).toBeTruthy();
  });
});
